name: Format and check

on:
  workflow_dispatch:
  push:
    paths:
      - '*.js'
      - '*.css'
      - '*.html'
      - '.prettierrc'
      - '.github/workflows/format-check.yml'
      - 'refactor_grill.py'
  pull_request:
    paths:
      - '*.js'
      - '*.css'
      - '*.html'
      - '.prettierrc'
      - '.github/workflows/format-check.yml'

permissions:
  contents: write

jobs:
  prettier:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Refactor Grill Me responsibilities
        if: github.event_name != 'pull_request'
        run: python refactor_grill.py
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Format source files
        if: github.event_name != 'pull_request'
        run: npx --yes prettier@3 --write "*.js" "*.css" "*.html"
      - name: Commit formatting changes
        if: github.event_name != 'pull_request'
        run: |
          rm -f refactor_grill.py
          rm -f refactor-trigger.txt
          if git diff --quiet; then
            echo "No formatting changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "refactor: separate Grill Me responsibilities [skip ci]"
          git push
      - name: Check formatting
        run: npx --yes prettier@3 --check "*.js" "*.css" "*.html"
