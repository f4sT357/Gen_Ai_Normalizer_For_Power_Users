# GANFPU 要改善リスト

対象ブランチ: `refactor/state-separation`

最終更新: 2026-09-07

このファイルは、現時点で確認できている不具合・設計上の改善点・検証項目をまとめた作業バックログ。優先度は「ユーザーが正常に使えること」→「LLM呼び出し削減」→「状態分離・責務整理」の順で扱う。

## P0 — 今すぐ修正

### 1. Requirement Discovery の不正LLM応答から安全に復旧する

- [x] `core/requirement-discovery.js` にJSONパース失敗時の保守的なローカルフォールバックを追加。
- [x] `ask_user` 以外の不正なアクションを受理しない。
- [x] フォールバック時も既存のRequirement Modelを完了扱いにしない。
- [ ] GitHub Pages上の最新コードで、`User Safety: safe` のような非JSON応答から実際に次の質問へ復旧できることをブラウザログで確認。
- [ ] malformed response時に追加LLMリトライを行わず、無駄なLLM呼び出しを増やさない現在方針が妥当か検証。

### 2. Requirement Discovery のLLM出力形式を強化する

- [ ] JSON Schema / structured output が利用可能なプロバイダでは構造化出力を利用できるようにする。
- [ ] 現在のプロンプト依存だけのJSON強制から、可能な範囲で機械的な形式保証へ移行する。
- [ ] OpenRouter Free / LM Studioなどプロバイダ差を考慮する。
- [ ] structured output非対応時は現在のJSON.parse経路を維持しつつ、失敗時に安全に扱う。

### 3. テンプレート取得パスの404を解消する

- [x] GitHub Pages / Jekyllによる`.md`静的ファイル取得問題に対して、ルート`.nojekyll`を追加。
- [ ] GitHub Pages上の最新デプロイで `data/templates/ja/*.md` の404が消えることをブラウザログで確認。
- [ ] `data/templates.json` の定義と実テンプレート配置が一致していることを確認。
- [ ] Service Workerのpre-cache対象が実ファイルパスと一致していることを確認。

## P1 — 次に修正

### 4. Service Workerの更新・キャッシュ戦略を検証する

- [x] `CACHE_NAME` を `ganfpu-v3` から `ganfpu-v4` に更新。
- [x] 古いCache Storageをactivate時に削除する処理を維持。
- [x] Cache-firstからNetwork-firstへ変更。
- [x] 存在しないテンプレートパスのpre-cacheを削除。
- [x] stale cacheが今回の起動不能原因であることをキャッシュ削除後の実動作で確認。
- [ ] GitHub Pages上で新Service Workerが実際に更新されることを確認。
- [ ] JS更新時に新旧JSが混在しないことを確認。
- [ ] オフライン時のフォールバックが正常に動作することを確認。
- [ ] 将来的にはHTML/JS/CSSなど更新頻度の高い静的資産と、画像などの安定資産でキャッシュ戦略を分けるか検討する。

### 5. Requirement Modelへのdelta mergeを実ログで検証する

- [x] `addRequirement()` の戻り値 `{ model, requirement, added, reason }` を呼び出し側が正しく扱うよう修正。
- [x] delta extraction結果がobject-shapedの場合も受理する。
- [x] delta requirementをRequirement Modelへmergeする経路を実装。
- [ ] ユーザー回答から `f-context` / `f-constraint` / `f-format` 等が実際に追加されることを確認。
- [ ] 同一identityの重複追加が発生しないことを確認。
- [ ] `confirmed` / `unknown` / `not_required` のsource検証が維持されることを確認。
- [ ] `unknown` をLLMから完全排除しない。ユーザーが「分からない」「決められない」等と回答した場合には `unknown` を生成できることを確認。

### 6. Recommendation / Researchで必要要件だけを聞くことを検証する

- [x] recommendation / research のRequirement Discoveryが会話履歴を参照して質問を生成するようにした。
- [x] 初回recommendation / researchターンでは、要件収集前にKnowledge Discoveryを実行しないようにした。
- [ ] recommendation / research では、目的達成に必要なpreferences / constraintsのみを質問することを実ログで確認。
- [ ] 10フィールドを機械的に埋めないことを確認。
- [ ] knowledgeで十分な知識がある場合のshort-circuitが機能することを確認。
- [ ] recommendation / researchでは必要なRequirement Discoveryを経由することを確認。
- [ ] `f-lang` を会話言語から自動確定しないことを確認。

### 7. Intent判定のLLM呼び出し削減を検証する

- [x] heuristicで明確に判定できるケースではIntent LLMを呼ばない経路を実装。
- [x] heuristicで `recommendation` / `research` / `knowledge` / `transformation` を判定できる場合はLLMをバイパスする。
- [x] ambiguous / unknownケースではLLMによるIntent判定を残す。
- [ ] recommendation / research / knowledge / transformation の境界ケースを実ログで検証。
- [ ] `unknown` を完全にLLMから外さないことを回帰確認。

## P1 — LLMコスト・遅延削減

### 8. 不要なRequirement Extractionをさらに削減する

- [x] 初回ターンで既に確定しているf-taskを再抽出しない。
- [x] 初回ターンの冗長なrequirement extractionをスキップする経路を追加。
- [ ] delta extractionをユーザー回答後など、本当に新情報が発生したタイミングに限定する。
- [ ] 同一ユーザーメッセージに対する重複抽出を発生させないことを実ログで確認。
- [ ] 抽出対象を必要なfield / dimensionへ絞れるか検討する。

### 9. 不要なRequirement Discoveryを削減する

- [x] conservative local completion gateを追加。
- [x] Requirement Modelが十分な状態である場合に、不要なDiscoveryを避けられる経路を追加。
- [ ] recommendation / research等で、明らかに不足している要件をローカルに判定できるケースを増やせるか検討。
- [ ] ただし「質問不要」と「情報不足だがLLMに判断させる必要がある」を混同しない。
- [ ] conservative gateによる誤完了が発生しないことを実ログで確認。

### 10. Knowledge Discoveryの呼び出し条件を最適化する

- [x] knowledge taskでknowledgeが十分な場合のshort-circuitを維持。
- [x] 初回recommendation / researchターンでは、Requirement Discovery前のKnowledge Discoveryをスキップ。
- [ ] knowledge / research / recommendation の各タスクで、knowledge discoveryが本当に必要な場合だけ実行することを実ログで確認。
- [ ] 既存knowledgeが十分な場合は再検索・再要約を避ける。
- [ ] recommendation / researchでRequirement Discoveryとの順序・依存関係を検証する。

### 11. LLM呼び出しの直列化・重複を確認する

- [x] `grill-debug.js` v5でLLM / network / core stepを横断して追跡できる診断ログを追加。
- [ ] 1ターン内で同じ目的のLLM呼び出しが複数発生していないか実ログで確認する。
- [ ] Intent / extraction / knowledge / discoveryそれぞれの責務を明確にする。
- [ ] 「LLMを止める」のではなく「LLMに考えさせる仕事そのものを減らす」を基準にする。

## P2 — 状態分離・責務整理

### 12. Normal ModeとPower Modeの状態分離を回帰テストする

- [ ] Normal Mode開始時に過去のPower Mode Prompt Specificationが混入しない。
- [ ] Normal Mode interview中にPower Mode stateが暗黙に変更されない。
- [ ] `Apply` を実行した場合だけPower Mode fieldsが変更される。
- [ ] Normal Modeを複数回連続実行しても前回Requirement Model / Discovery stateを引き継がない。

### 13. Normal Modeの入口境界を維持する

- [x] `normal-intent` → `ganfpuStartGrill(intent)` の境界を実装。
- [x] `normal-mode.js` からPower Modeの自動コピー経路を削除。
- [ ] Normal Mode UIがPower Modeのフォーム値を参照してRequirement Modelを汚染しないことを回帰確認。
- [ ] `normal-mode.js` はNormal Mode UI / mode shellに責務を限定する。

### 14. Grill Controller / Orchestratorの責務を維持する

- [x] UIイベント処理を`grill-controller.js`に集約。
- [x] Requirement interviewの状態遷移を`core/orchestrator.js`に集約。
- [x] Prompt CompilerをRequirement ModelからPromptを生成する純粋変換として維持。
- [x] Power ModeのPrompt SpecificationをNormal Modeのinterview stateとして再利用しない構造に整理。
- [ ] 今後の変更で責務が逆流しないことを回帰確認。

## P2 — エラー処理・可観測性

### 15. Requirement Discovery失敗時の診断情報を改善する

- [x] `grill-debug.js`でLLM response / error / elapsed_msを記録。
- [x] Requirement Discoveryの非JSON応答をログ上で特定できる。
- [ ] `null`だけでなく、失敗理由（parse failure / invalid action / missing adapter等）を内部的に識別できるようにする。
- [ ] ユーザー向けエラーとdebug logを分離する。
- [ ] フォールバック発動をdebug log上で明示できるようにする。

### 16. Core stepの診断を確認する

- [x] `grill-debug.js` v5で`llm_calls` / `network_calls` / `core_steps`を記録。
- [ ] v5ログだけで「どのLLM呼び出しが何の責務だったか」を追跡できることを確認。
- [ ] Core stepが空になるケースと、ControllerからCoreへ到達していないケースを区別できるようにする。

## P3 — テスト整備

### 17. Normal Modeの代表ケースを固定テストする

- [ ] 単純な変換: 「この文章を短くして」
- [ ] 知識: 「○○について教えて」
- [ ] 推薦: 「サングラスのおすすめ教えて」
- [ ] 調査: 「○○について調べて比較して」
- [ ] 曖昧な依頼: Intent LLMが必要になるケース
- [ ] ユーザーが条件を追加するケース
- [ ] ユーザーが「分からない」と答えるケース

### 18. Requirement Modelの状態テスト

- [ ] candidate → confirmed
- [ ] candidate → unknown
- [ ] candidate → not_required
- [ ] confirmedの重複防止
- [ ] user sourceなしのconfirmed/unknown/not_requiredを拒否
- [ ] unknown / not_requiredにvalueを設定しない

### 19. Service Worker回帰テスト

- [ ] 新規アクセス
- [ ] コード更新後の再アクセス
- [ ] 古いService Workerが残った状態からの更新
- [ ] offline fallback
- [ ] GitHub Pages deployment後の更新

### 20. OpenRouter Freeのレート制限で1ターンの処理が停止する問題

- [ ] 1ターン内のRequirement Extraction / Knowledge Discovery / Requirement Discoveryの呼び出し順序と必要性をコードで確認する。
- [ ] 同一ターンで不要な直列LLM呼び出しを削減し、OpenRouter Freeの日次上限に到達しにくい構造にする。
- [ ] HTTP 429をLLM失敗として一括処理せず、rate limitとして診断ログ上で識別する。
- [ ] 429発生時にRequirement Modelを未完了のまま安全に保持し、ユーザーに再試行可能な状態を返す。
- [ ] 「LLMを止める」のではなく、決定的にローカル処理できる仕事をLLMから外す方針で改善する。

## 現時点で確認済みの実問題

### A. Service Workerの古いキャッシュによる新旧JS混在

- 実際にキャッシュ削除後、アプリが正常動作することを確認済み。
- よって旧キャッシュが今回の起動不能の原因だったと判断できる。
- 修正コミット: `d33aae87290da2ec563a96917f501669c6cd0c3b`

### B. Requirement Discoveryの非JSON応答

- 2026-09-06の実行ログで再現。
- LLM #1が `User Safety: safe` を返した。
- 旧実行コードでは`requirement-discovery.js`のJSON.parse失敗によりアクション生成に失敗した。
- 現在は保守的なローカルフォールバックを実装済みだが、GitHub Pages上の最新コードでの実動作確認が未完了。

### C. テンプレートの404

- 2026-09-06の実行ログで12件確認。
- `.nojekyll`を追加してGitHub Pages上での`.md`静的配信問題に対処済み。
- 最新デプロイ後に404が解消したことをブラウザログで確認する必要がある。

### D. Evidence backendのGitHub Pages実行時接続失敗

- 2026-09-06の実行ログで `http://localhost:8787/evidence/search?...` への接続失敗を確認。
- GitHub Pagesは静的ホスティングのため、ローカルの`localhost:8787` Evidence backendへ接続できない。
- Requirement Discovery自体はこの失敗で必ずしも停止しないが、Evidence機能をGitHub Pages上で利用する設計としては別途整理が必要。
- [ ] deployed環境でEvidence backendを必要とする機能の責務・fallbackを整理する。

### E. OpenRouter FreeのHTTP 429によるRequirement Discovery停止

- 2026-09-07の実行ログで再現。
- `Requirement Extraction` 成功後、追加のRequirement Discovery呼び出しがHTTP 429になった。
- エラー本文: `Rate limit exceeded: free-models-per-day`。
- 429自体は約66msで返っており、Response Body Stallとは別問題。
- 同一ターン内で複数のLLM処理が直列実行されるため、Freeモデルの日次上限到達時にユーザー向け処理が停止する。
- まずコード上の呼び出し条件・順序を確認し、不要なLLM処理を減らす。

## 次にやること

1. GitHub Pagesの最新デプロイで再実行し、`data/templates/ja/*.md` の404が解消したか確認する。
2. `サングラスのおすすめ教えて` で、heuristic intent → Requirement Extraction → Requirement Discoveryの実際のログを確認する。
3. Requirement Discoveryが不正JSONを返した場合でも、ローカルフォールバック質問が表示されることを確認する。
4. ユーザー回答（例: `予算は1万円以下`）から`f-constraint`がRequirement Modelへ`confirmed`として追加されることを確認する。
5. recommendation / researchで、必要な要件収集前に不要なKnowledge Discoveryが走らないことを確認する。
6. 実ログを基に、次のLLM削減ポイント（Requirement Extraction / Discovery / Knowledge Discovery）を優先順位付けする。

## 方針

1. 正常動作を回復する。
2. LLMの不正出力を安全に処理する。
3. 404等の不要なネットワークエラーを消す。
4. Requirement Model / Discovery / Knowledgeの無駄なLLM呼び出しを減らす。
5. Normal Mode / Power Modeの状態境界を維持する。
6. `unknown` は曖昧ケースのためにLLM経路から残す。
7. 「thinkingを強制停止する」のではなく、LLMへ渡す仕事そのものを減らす。
