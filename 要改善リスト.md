# GANFPU 要改善リスト

対象ブランチ: `refactor/state-separation`

最終更新: 2026-09-07

このファイルは、現時点で確認できている不具合・設計上の改善点・検証項目をまとめた作業バックログ。優先度は「ユーザーが正常に使えること」→「LLM呼び出し削減」→「状態分離・責務整理」の順で扱う。

## P1 — LLMコスト・遅延削減

### 12. OpenRouter Freeのレート制限で1ターンの処理が停止する問題

- [ ] 1ターン内のRequirement Extraction / Knowledge Discovery / Requirement Discoveryの呼び出し順序と必要性をコードで確認する。
- [ ] 同一ターンで不要な直列LLM呼び出しを削減し、OpenRouter Freeの日次上限に到達しにくい構造にする。
- [ ] HTTP 429をLLM失敗として一括処理せず、rate limitとして診断ログ上で識別する。
- [ ] 429発生時にRequirement Modelを未完了のまま安全に保持し、ユーザーに再試行可能な状態を返す。
- [ ] 「LLMを止める」のではなく、決定的にローカル処理できる仕事をLLMから外す方針で改善する。

## 現時点で確認済みの実問題

### E. OpenRouter FreeのHTTP 429によるRequirement Discovery停止

- 2026-09-07の実行ログで再現。
- `Requirement Extraction` 成功後、追加のRequirement Discovery呼び出しがHTTP 429になった。
- エラー本文: `Rate limit exceeded: free-models-per-day`。
- 429自体は約66msで返っており、Response Body Stallとは別問題。
- 同一ターン内で複数のLLM処理が直列実行されるため、Freeモデルの日次上限到達時にユーザー向け処理が停止する。
- まずコード上の呼び出し条件・順序を確認し、不要なLLM処理を減らす。

## 方針

- 「thinkingを強制停止する」のではなく、LLMへ渡す仕事そのものを減らす。
- `unknown` は曖昧ケースのためにLLM経路から残す。
